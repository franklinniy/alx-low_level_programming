#include <stdio.h>

/**
 * main - Entry point of the program.
 *
 * Return: Always 0 (Success).
 *
 * This program prints a message using 'printf' with proper grammar
 * and ends with a new line. It returns 0 to indicate successful execution.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
